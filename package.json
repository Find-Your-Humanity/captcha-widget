{
  "name": "captcha-ui",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:cdn": "npm run build && npm run bundle:cdn",
    "bundle:cdn": "node scripts/build-cdn.js",
    "deploy:cdn": "npm run build:cdn && node scripts/deploy-cdn.js",
    "deploy:kakao": "npm run build:cdn && node scripts/deploy-kakao-cdn.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.0",
    "aws-sdk": "^2.1284.0",
    "dotenv": "^16.0.0",
    "terser": "^5.16.0",
    "fs-extra": "^11.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
